#!/bin/bash -e

# If running the Rails server then create/migrate the database and load seeds.
if [ "$1" = "./bin/rails" ] && [ "$2" = "server" ]; then
  ./bin/rails db:prepare
  ./bin/rails db:seed
fi

exec "${@}"
