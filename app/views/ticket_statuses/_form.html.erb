<%= form_with(model: ticket_status) do |form| %>
  <% if ticket_status.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ticket_status.errors.count, "error") %> prohibited this ticket_status from being saved:</h2>

      <ul>
        <% ticket_status.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :is_default, style: "display: block" %>
    <%= form.checkbox :is_default %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
