<article id="<%= dom_id user %>" class="col-12 col-md-6 col-xl-4">
  <div class="card border-0 shadow-sm h-100">
    <div class="card-body p-4">
      <div class="d-flex justify-content-between align-items-start gap-3 mb-3">
        <h2 class="h5 mb-0"><%= user.name %></h2>
        <span class="badge text-bg-light border text-capitalize"><%= user.user_type %></span>
      </div>

      <dl class="mb-0">
        <div class="py-2 border-bottom">
          <dt class="text-secondary fw-medium">Email</dt>
          <dd class="mb-0 fw-semibold text-break"><%= user.email %></dd>
        </div>

        <div class="py-2 border-bottom">
          <dt class="text-secondary fw-medium">Escopos</dt>
          <dd class="mb-0"><%= user.scopes.map(&:title).join(", ").presence || "-" %></dd>
        </div>

        <div class="py-2">
          <dt class="text-secondary fw-medium">Apartamentos</dt>
          <dd class="mb-0"><%= user.apartments.map(&:identificator).join(", ").presence || "-" %></dd>
        </div>
      </dl>

      <div class="mt-4 d-flex gap-2">
        <%= link_to "Ver", user, class: "btn btn-outline-primary btn-sm" %>
        <%= link_to "Editar", edit_user_path(user), class: "btn btn-light btn-sm" %>
      </div>
    </div>
  </div>
</article>
