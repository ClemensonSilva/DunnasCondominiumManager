<nav class="navbar navbar-expand-lg w-100" style="background-color: #1b4d5c;">
  <div class="container-fluid px-3 px-md-4">
    <%= link_to "Dunnas Condominium", unauthenticated_root_path, class: "navbar-brand text-white fw-semibold" %>

    <div class="ms-auto d-flex gap-2">
      <% unless controller_name == "sessions" %>
        <%= link_to "Login", new_user_session_path, class: "btn btn-light text-dark fw-semibold" %>
      <% end %>

      <% if controller_name != "registrations" %>
        <%= link_to "Sign up", new_user_registration_path, class: "btn btn-outline-light fw-semibold" %>
      <% end %>
    </div>
  </div>
</nav>
